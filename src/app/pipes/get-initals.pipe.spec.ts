import { GetInitalsPipe } from './get-initals.pipe';

describe('GetInitalsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetInitalsPipe();
    expect(pipe).toBeTruthy();
  });
});
